# QuillViewer - Пагинация и UI улучшения ✅

## Что реализовано

### 1. **Floating пагинация в нижнем левом углу** 🎯
- ✅ Компактный дизайн с круглыми кнопками
- ✅ Расположение: bottom-left corner
- ✅ Фон: цвет primary с тенью
- ✅ Компактный индикатор: "2/5" вместо "Страница 2 / 5"
- ✅ Маленькие иконки (16px) для экономии места
- ✅ Полупрозрачность для disabled кнопок

### 2. **Скрытие нижней навигации при чтении** 📖
- ✅ Tab bar автоматически скрывается при открытии viewer
- ✅ Работает для ВСЕХ форматов (JSON Quill, PDF, MD и т.д.)
- ✅ Tab bar возвращается при выходе из viewer
- ✅ Плавная анимация показа/скрытия

## UI Layout

### Floating пагинация (нижний левый угол)
```
┌────────────────────────────────────┐
│                                    │
│   📄 Контент документа             │
│                                    │
│                                    │
│  ┌────────┐                        │
│  │ ◄ 2/5 ►│ ← Компактно!           │
└──└────────┘────────────────────────┘
     ↑ Bottom-left floating button
```

### С скрытой навигацией
```
┌────────────────────────────────────┐
│ [←] [🔍 Поиск...]                  │ ← Header
├────────────────────────────────────┤
│                                    │
│   📄 Полный экран для чтения       │
│                                    │
│   Никакой навигации снизу! 🎉      │
│                                    │
│  ┌────────┐                        │
│  │ ◄ 2/5 ►│                        │
└──└────────┘────────────────────────┘
    ↑ Пагинация всегда видна
```

## Стили floating пагинации

```typescript
floatingPagination: {
  position: 'absolute',
  bottom: 20,           // 20px от низа
  left: 16,             // 16px от левого края
  flexDirection: 'row',
  backgroundColor: colors.primary,
  borderRadius: 20,     // Закругленный pill-shape
  paddingVertical: 6,
  paddingHorizontal: 8,
  elevation: 4,         // Тень на Android
  shadowColor: '#000',  // Тень на iOS
  shadowOpacity: 0.25,
}
```

## Логика скрытия tab bar

### В HomeScreen.tsx
```typescript
useEffect(() => {
  if (navigation) {
    navigation.setOptions({
      tabBarStyle: currentScreen === 'viewer' 
        ? { display: 'none' }  // Скрыть при просмотре
        : {
            // Показать при других экранах
            backgroundColor: colors.surface,
            borderTopColor: colors.border,
            // ... остальные стили
          }
    });
  }
}, [currentScreen, navigation, colors]);
```

### Когда скрывается?
- ✅ При открытии любого материала (JSON, PDF, MD, DOCX и т.д.)
- ✅ `currentScreen === 'viewer'`

### Когда показывается?
- ✅ На главном экране (список предметов)
- ✅ На экране типов материалов
- ✅ На экране списка материалов
- ✅ `currentScreen !== 'viewer'`

## Преимущества

### Floating пагинация
1. **Не занимает место в header** - header остается чистым
2. **Всегда видна** - не скрывается при скролле
3. **Компактная** - маленький размер не мешает контенту
4. **Удобная** - в зоне большого пальца на смартфонах

### Скрытие навигации
1. **Больше места для чтения** - ~70px дополнительного пространства
2. **Меньше отвлечений** - фокус на контенте
3. **Профессиональный вид** - как в книжных приложениях
4. **Работает везде** - для всех типов документов

## Размеры

### Floating пагинация
- Высота: ~34px (6px padding + 16px icon + 6px padding)
- Ширина: ~70-80px (зависит от номера страницы)
- Иконки: 16x16px
- Текст: 12px, bold
- От края: 16px (left), 20px (bottom)

### Tab bar (когда видна)
- Высота: 70px
- Padding: 8px top + 8px bottom
- Иконки: стандартный размер
- Текст: 12px

## Тестирование

### Проверить floating пагинацию:
1. Открой любой JSON Quill документ
2. Посмотри в нижний левый угол
3. Видишь компактную кнопку с "◄ 2/5 ►"
4. Нажми ◄ или ► для навигации
5. Кнопка остается видимой при скролле

### Проверить скрытие навигации:
1. Открой главный экран (список предметов)
2. Внизу видна навигация (Главная, AI, Магазин, Профиль)
3. Открой любой материал
4. Навигация исчезла! 🎉
5. Нажми "Назад"
6. Навигация вернулась

## Технические детали

### Z-index и позиционирование
- Floating кнопка: `position: 'absolute'`
- Z-index не нужен (WebView ниже по дереву)
- Кнопка всегда поверх контента

### Производительность
- Без анимаций (instant show/hide)
- Легковесные стили
- Не влияет на скролл

### Совместимость
- ✅ iOS
- ✅ Android
- ✅ Web (если нужно)

## Файлы изменений

1. **src/components/viewers/QuillViewer.tsx**
   - Добавлена floating пагинация
   - Убрана пагинация из header
   - Новые стили

2. **src/screens/HomeScreen.tsx**
   - Добавлен useEffect для управления tab bar
   - Скрытие/показ в зависимости от currentScreen

## Что дальше?

Возможные улучшения:
- [ ] Swipe жесты для переключения страниц
- [ ] Haptic feedback при смене страницы
- [ ] Кастомизация положения пагинации (настройки)
- [ ] Анимация появления/исчезания floating кнопки
