/**
 * DEPRECATED: This file is no longer used.
 * 
 * All contexts must be imported directly from their individual files to avoid module conflicts:
 * 
 *   import { ThemeProvider, useTheme } from '../contexts/ThemeContext';
 *   import { LanguageProvider, useLanguage } from '../contexts/LanguageContext';
 *   import { AuthProvider, useAuth } from '../contexts/AuthContext';
 * 
 * DO NOT use: import { ... } from '../contexts'
 */

// Exports removed to prevent module duplication issues
// If you see errors, update your imports to use direct paths shown above

